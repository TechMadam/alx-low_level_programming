#include <stdio.h>
#include "mark.h"
/**
 * add - adds two integers
 * @a: first integer to add
 * @b: second integer to add
 *
 * Return: sum of the two integers
 */
int add(int a, int b)
{
        return (a + b);
}
