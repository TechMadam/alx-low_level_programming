#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 *
 * main - entry point
 *
 * return- always return 0
 *
 */

int main()
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
